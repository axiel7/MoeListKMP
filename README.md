# MoeList Multiplatform

Kotlin multiplatform version of MoeList (Android, iOS, Desktop)

WIP

# Building
Put the following in a new ./private.properties file:

```properties
CLIENT_SECRET=your_mal_api_client_secret
```

The client secret is only needed for the desktop variant. For Android/iOS you can leave it blank.